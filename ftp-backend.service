[Unit]
Description=FastAPI backend for FTP project
After=network.target

[Service]
User=ubuntu
WorkingDirectory=/workspaces/FTP
ExecStart=/usr/bin/env uvicorn main:app --host 0.0.0.0 --port 8000 --reload
Restart=always

[Install]
WantedBy=multi-user.target
